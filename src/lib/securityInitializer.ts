import { initializeEnhancedSecurity } from './enhancedSecurityInitializer';

// Keep backwards compatibility while using enhanced security
export const initializeSecurity = initializeEnhancedSecurity;
